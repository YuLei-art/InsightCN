# 数据层（已验证可运行，无需 LLM Key）
pandas>=2.2.0
pydantic>=2.0.0
requests>=2.31.0

# A 阶段数据底座：本机可直接用 AkShare；沙箱环境用原生 requests 直连东方财富
akshare>=1.15.0

# 完整多智能体运行需额外安装（默认模型 gpt-4.1，可选 DeepSeek）：
#   langchain-openai>=0.3.5
#   openai>=1.0.0
#   httpx>=0.27.0
# 详细依赖见上游 pyproject.toml
